<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.hage.platform.component</groupId>
        <artifactId>configuration-parent</artifactId>
        <version>0.2.0-SNAPSHOT</version>
    </parent>

    <groupId>org.hage.platform.components</groupId>
    <artifactId>configuraion-core</artifactId>

    <name>HAgE::Platform::Configuration::Core</name>
    <description>The HAgE configuration loader and provider interfaces</description>

    <dependencies>
        <dependency>
            <groupId>org.hage.platform.component.config</groupId>
            <artifactId>internal-config-data</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.hage.platform.component.config</groupId>
            <artifactId>config-api</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.hage.platform.components</groupId>
            <artifactId>habitat-parent</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.hage.platform.components.habitat</groupId>
            <artifactId>habitat-structure</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

</project>