<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>services</artifactId>
        <groupId>org.jage.services</groupId>
        <version>0.1.0</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>configuration</artifactId>
    <name>AgE Configuration Service</name>
    <description>AgE service that configures nodes and the environment</description>

    <dependencies>
        <dependency>
            <groupId>org.jage.platform</groupId>
            <artifactId>configuration-xml</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>communication</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.jage.services</groupId>
            <artifactId>performance</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.jage.platform</groupId>
            <artifactId>arguments</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

</project>
