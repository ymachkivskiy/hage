<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.hage.services</groupId>
        <artifactId>services</artifactId>
        <version>0.2.0-SNAPSHOT</version>
    </parent>

    <artifactId>configuration</artifactId>

    <name>HAgE::Service::Configuration</name>
    <description>HAgE service that configures nodes and the environment</description>

    <dependencies>

        <dependency>
            <groupId>org.hage.platform.components</groupId>
            <artifactId>configuraion-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.hage.platform.components.config</groupId>
            <artifactId>loader-xml</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.hage.services</groupId>
            <artifactId>communication</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.hage.platform.component</groupId>
            <artifactId>rate</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.hage.platform.common</groupId>
            <artifactId>event-bus</artifactId>
            <version>${project.version}</version>
        </dependency>

    </dependencies>

</project>
